<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Controlpaseosmascotas" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Esta línea es solo para evitar errores XML, no se usará en Compose -->
        <item name="android:statusBarColor" tools:targetApi="21">@android:color/transparent</item>
    </style>
</resources>
